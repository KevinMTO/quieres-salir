# Patatina Date Invite

A small static website for a personal date invitation.

## GitHub Pages

Upload the contents of this folder to a GitHub repository with `index.html` at the repository root. Then enable GitHub Pages from `Settings` > `Pages` using the `main` branch and `/root` folder.
